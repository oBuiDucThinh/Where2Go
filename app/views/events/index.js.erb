<% js = escape_javascript(
  render(partial: 'events/event_list_ajx', locals: { events: @events })
) %>
var temp = $('#filterrific_results').find('.active').attr('id');
$('#filterrific_results').html('<%= js %>');
$('#' + temp).addClass('in active');
