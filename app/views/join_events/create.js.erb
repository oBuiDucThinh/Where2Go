if(<%= @temp %>){
  $('#join_<%= @event_id %>').addClass('fa-sign-in-press');
} else {
  $('#join_<%= @event_id %>').removeClass('fa-sign-in-press');
}
$('#join_count_<%= @event_id %>').html('<%= @current_event.join_events.count %>');
